import { release } from "./release";
import { artist } from "./artist";
import { event } from "./event";
import { blockContent } from "./blockContent";

export const schemaTypes = [release, artist, event, blockContent];
